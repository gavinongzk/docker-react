language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t gavinong/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true gavinong/docker-react npm run test
